# 权限模式系统

<cite>
**本文档引用的文件**
- [permissions.md](file://apps/electron/resources/docs/permissions.md)
- [default.json](file://apps/electron/resources/permissions/default.json)
- [permissions-config.ts](file://packages/shared/src/agent/permissions-config.ts)
- [mode-types.ts](file://packages/shared/src/agent/mode-types.ts)
- [mode-manager.ts](file://packages/shared/src/agent/mode-manager.ts)
- [validators.ts](file://packages/shared/src/config/validators.ts)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 简介

权限模式系统是Craft Agents中一个关键的安全控制机制，采用三层权限模式设计：Explore（探索）、Ask（询问）和Auto（自动）。该系统通过分层权限控制确保在不同操作场景下提供适当的安全保障。

系统的核心设计理念是"最小权限原则"和"渐进式授权"。Explore模式提供完全只读访问，Ask模式在执行危险操作前要求用户确认，Auto模式允许完全自主执行。这种设计既保证了安全性，又提供了灵活的操作体验。

## 项目结构

权限模式系统主要分布在以下位置：

```mermaid
graph TB
subgraph "权限配置文档"
A[permissions.md<br/>权限配置指南]
B[default.json<br/>默认权限配置]
end
subgraph "核心实现"
C[permissions-config.ts<br/>权限配置管理]
D[mode-types.ts<br/>模式类型定义]
E[mode-manager.ts<br/>模式管理器]
F[validators.ts<br/>配置验证器]
end
subgraph "用户界面"
G[TopBar.tsx<br/>顶部栏权限按钮]
H[PermissionsSettingsPage.tsx<br/>权限设置页面]
end
A --> C
B --> C
C --> D
D --> E
E --> F
E --> G
E --> H
```

**图表来源**
- [permissions.md:1-339](file://apps/electron/resources/docs/permissions.md#L1-L339)
- [default.json:1-950](file://apps/electron/resources/permissions/default.json#L1-L950)
- [permissions-config.ts:1-935](file://packages/shared/src/agent/permissions-config.ts#L1-L935)
- [mode-types.ts:1-341](file://packages/shared/src/agent/mode-types.ts#L1-L341)

**章节来源**
- [permissions.md:1-339](file://apps/electron/resources/docs/permissions.md#L1-L339)
- [default.json:1-950](file://apps/electron/resources/permissions/default.json#L1-L950)

## 核心组件

### 三层权限模式

系统定义了三种核心权限模式：

| 模式 | 内部键 | 描述 | 主要特征 |
|------|--------|------|----------|
| Explore | safe | 探索模式（只读） | 完全只读，阻止所有写操作，不提示 |
| Ask | ask | 询问模式 | 危险操作前提示，其他操作自由 |
| Execute | allow-all | 执行模式（自动） | 全面执行，无提示 |

### 权限配置层次

系统采用三级权限配置层次结构：

1. **应用级默认配置**：`~/.craft-agent/permissions/default.json`
2. **工作区级配置**：`workspaceRoot/permissions.json`
3. **源级配置**：`workspaceRoot/sources/{source}/permissions.json`

每级配置都是累加式的，后加载的配置只能扩展权限，不能减少权限。

**章节来源**
- [mode-types.ts:24-52](file://packages/shared/src/agent/mode-types.ts#L24-L52)
- [permissions-config.ts:7-12](file://packages/shared/src/agent/permissions-config.ts#L7-L12)

## 架构概览

```mermaid
graph TB
subgraph "用户界面层"
UI[用户界面]
BTN[权限模式按钮]
SETTINGS[设置页面]
end
subgraph "模式管理层"
MM[ModeManager<br/>模式管理器]
MS[ModeState<br/>模式状态]
SUB[订阅系统]
end
subgraph "权限配置层"
PC[PermissionsConfigCache<br/>配置缓存]
WC[WorkspaceConfig<br/>工作区配置]
SC[SourceConfig<br/>源配置]
DC[DefaultConfig<br/>默认配置]
end
subgraph "安全检查层"
BC[BashValidator<br/>Bash验证器]
PCV[PowerShellValidator<br/>PowerShell验证器]
AC[API检查器]
end
UI --> BTN
BTN --> MM
SETTINGS --> MM
MM --> MS
MM --> SUB
MS --> PC
PC --> WC
PC --> SC
PC --> DC
PC --> BC
PC --> PCV
PC --> AC
```

**图表来源**
- [mode-manager.ts:230-376](file://packages/shared/src/agent/mode-manager.ts#L230-L376)
- [permissions-config.ts:574-934](file://packages/shared/src/agent/permissions-config.ts#L574-L934)

## 详细组件分析

### 权限配置管理系统

权限配置管理系统负责管理三层权限配置并进行合并处理：

```mermaid
classDiagram
class PermissionsConfigCache {
-Map~string, PermissionsCustomConfig| workspaceConfigs
-Map~string, PermissionsCustomConfig| sourceConfigs
-Map~string, MergedPermissionsConfig| mergedConfigs
-PermissionsCustomConfig defaultConfig
+getDefaultConfig() PermissionsCustomConfig
+getWorkspaceConfig(path) PermissionsCustomConfig
+getSourceConfig(path, slug) PermissionsCustomConfig
+getMergedConfig(context) MergedPermissionsConfig
+invalidateDefaults() void
+invalidateWorkspace(path) void
+invalidateSource(path, slug) void
}
class PermissionsCustomConfig {
+PatternWithComment[] allowedBashPatterns
+string[] allowedMcpPatterns
+ApiEndpointRule[] allowedApiEndpoints
+string[] allowedWritePaths
+BlockedCommandHintRule[] blockedCommandHints
}
class MergedPermissionsConfig {
+Set~string| blockedTools
+CompiledBashPattern[] readOnlyBashPatterns
+CompiledBlockedCommandHint[] blockedCommandHints
+RegExp[] readOnlyMcpPatterns
+CompiledApiEndpointRule[] allowedApiEndpoints
+string[] allowedWritePaths
+string displayName
+string shortcutHint
+PermissionPaths permissionPaths
}
PermissionsConfigCache --> PermissionsCustomConfig : manages
PermissionsConfigCache --> MergedPermissionsConfig : creates
MergedPermissionsConfig --> PermissionsCustomConfig : merges from
```

**图表来源**
- [permissions-config.ts:574-934](file://packages/shared/src/agent/permissions-config.ts#L574-L934)
- [mode-types.ts:236-270](file://packages/shared/src/agent/mode-types.ts#L236-L270)

### 模式管理器

模式管理器负责每个会话的权限模式状态管理：

```mermaid
sequenceDiagram
participant User as 用户
participant UI as 用户界面
participant MM as ModeManager
participant PC as 权限配置
participant Validator as 验证器
User->>UI : 切换权限模式
UI->>MM : setPermissionMode(mode)
MM->>MM : 更新模式状态
MM->>PC : 获取合并后的权限配置
PC->>Validator : 验证操作合法性
Validator-->>PC : 返回验证结果
PC-->>MM : 返回权限配置
MM-->>UI : 通知模式变更
UI-->>User : 更新界面显示
```

**图表来源**
- [mode-manager.ts:274-312](file://packages/shared/src/agent/mode-manager.ts#L274-L312)
- [mode-manager.ts:414-435](file://packages/shared/src/agent/mode-manager.ts#L414-L435)

### 权限检查流程

```mermaid
flowchart TD
Start([开始权限检查]) --> CheckMode{检查当前模式}
CheckMode --> |Explore| CheckExplore[检查Explore模式限制]
CheckMode --> |Ask| CheckAsk[检查Ask模式限制]
CheckMode --> |Execute| AllowAll[直接允许]
CheckExplore --> ValidateBash[验证Bash命令]
ValidateBash --> BashAllowed{Bash命令允许?}
BashAllowed --> |是| CheckMCP[检查MCP工具]
BashAllowed --> |否| RejectBash[拒绝Bash命令]
CheckAsk --> CheckDangerous{检查危险操作}
CheckDangerous --> |是| PromptUser[提示用户确认]
CheckDangerous --> |否| CheckMCP
PromptUser --> UserDecision{用户同意?}
UserDecision --> |是| CheckMCP
UserDecision --> |否| RejectOperation[拒绝操作]
CheckMCP --> ValidateMCP[验证MCP工具]
ValidateMCP --> MCPAllowed{MCP工具允许?}
MCPAllowed --> |是| CheckAPI[检查API调用]
MCPAllowed --> |否| RejectMCP[拒绝MCP工具]
CheckAPI --> ValidateAPI[验证API端点]
ValidateAPI --> APIAllowed{API调用允许?}
APIAllowed --> |是| Success[操作成功]
APIAllowed --> |否| RejectAPI[拒绝API调用]
AllowAll --> Success
RejectBash --> End([结束])
RejectMCP --> End
RejectAPI --> End
RejectOperation --> End
Success --> End
```

**图表来源**
- [mode-manager.ts:529-800](file://packages/shared/src/agent/mode-manager.ts#L529-L800)
- [permissions-config.ts:546-564](file://packages/shared/src/agent/permissions-config.ts#L546-L564)

**章节来源**
- [permissions-config.ts:574-934](file://packages/shared/src/agent/permissions-config.ts#L574-L934)
- [mode-manager.ts:230-523](file://packages/shared/src/agent/mode-manager.ts#L230-L523)

### 权限继承规则

系统实现了严格的权限继承规则：

```mermaid
graph TD
A[应用级默认配置] --> B[工作区级配置]
B --> C[源级配置]
C --> D[会话级权限]
subgraph "权限合并规则"
E[累加式合并]
F[后加载覆盖先加载]
G[不允许降级权限]
end
A --> E
B --> E
C --> E
E --> F
F --> G
```

**图表来源**
- [permissions.md:251-258](file://apps/electron/resources/docs/permissions.md#L251-L258)
- [permissions-config.ts:668-727](file://packages/shared/src/agent/permissions-config.ts#L668-L727)

**章节来源**
- [permissions.md:251-258](file://apps/electron/resources/docs/permissions.md#L251-L258)

### 权限模式切换机制

系统支持多种权限模式切换方式：

1. **键盘快捷键**：SHIFT+TAB循环切换
2. **用户界面**：顶部栏权限按钮
3. **程序化切换**：通过API接口

```mermaid
sequenceDiagram
participant User as 用户
participant Keyboard as 键盘输入
participant UI as 用户界面
participant MM as ModeManager
participant State as 模式状态
User->>Keyboard : 按下SHIFT+TAB
Keyboard->>UI : 触发事件
UI->>MM : cyclePermissionMode()
MM->>State : 获取当前模式
State-->>MM : 返回当前模式
MM->>MM : 计算下一个模式
MM->>State : 更新模式状态
State-->>MM : 确认状态更新
MM-->>UI : 通知模式变更
UI-->>User : 更新界面显示
```

**图表来源**
- [mode-manager.ts:414-435](file://packages/shared/src/agent/mode-manager.ts#L414-L435)

**章节来源**
- [mode-manager.ts:414-435](file://packages/shared/src/agent/mode-manager.ts#L414-L435)

## 依赖关系分析

```mermaid
graph TB
subgraph "外部依赖"
FS[文件系统]
OS[操作系统]
REGEX[正则表达式引擎]
end
subgraph "内部模块"
MODE_TYPES[mode-types.ts]
MODE_MANAGER[mode-manager.ts]
PERM_CONFIG[permissions-config.ts]
VALIDATORS[validators.ts]
end
subgraph "配置文件"
DEFAULT_JSON[default.json]
WORKSPACE_JSON[workspace permissions.json]
SOURCE_JSON[source permissions.json]
end
FS --> PERM_CONFIG
OS --> PERM_CONFIG
REGEX --> PERM_CONFIG
MODE_TYPES --> MODE_MANAGER
MODE_MANAGER --> PERM_CONFIG
PERM_CONFIG --> VALIDATORS
DEFAULT_JSON --> PERM_CONFIG
WORKSPACE_JSON --> PERM_CONFIG
SOURCE_JSON --> PERM_CONFIG
```

**图表来源**
- [permissions-config.ts:14-24](file://packages/shared/src/agent/permissions-config.ts#L14-L24)
- [mode-manager.ts:15-22](file://packages/shared/src/agent/mode-manager.ts#L15-L22)

**章节来源**
- [permissions-config.ts:14-24](file://packages/shared/src/agent/permissions-config.ts#L14-L24)
- [mode-manager.ts:15-22](file://packages/shared/src/agent/mode-manager.ts#L15-L22)

## 性能考虑

权限模式系统在设计时充分考虑了性能优化：

1. **配置缓存**：使用内存缓存避免重复解析配置文件
2. **懒加载**：仅在需要时加载和解析配置文件
3. **增量更新**：文件变更时仅失效相关缓存项
4. **正则表达式优化**：编译后的正则表达式复用

系统采用LRU缓存策略管理权限配置，确保在大量会话并发时仍能保持高性能。

## 故障排除指南

### 常见问题及解决方案

1. **权限配置无效**
   - 检查配置文件语法是否正确
   - 验证正则表达式格式
   - 确认文件路径正确

2. **模式切换不生效**
   - 检查会话ID是否正确
   - 确认权限配置文件权限
   - 验证配置缓存是否更新

3. **权限继承异常**
   - 检查配置文件加载顺序
   - 验证权限合并逻辑
   - 确认配置优先级

**章节来源**
- [validators.ts:1401-1444](file://packages/shared/src/config/validators.ts#L1401-L1444)

### 调试工具

系统提供了完整的调试功能：

- 模式状态诊断：获取详细的模式切换历史
- 权限检查日志：记录每次权限验证的详细信息
- 配置验证：实时验证配置文件的有效性

## 结论

权限模式系统通过精心设计的三层权限模型、严格的权限继承规则和高效的实现机制，为Craft Agents提供了强大而灵活的安全控制框架。系统不仅确保了操作的安全性，还为用户提供了流畅的使用体验。

该系统的核心优势在于：
- 渐进式授权设计，平衡了安全性和可用性
- 分层配置管理，支持细粒度的权限控制
- 高性能实现，支持大规模并发使用
- 完善的错误处理和调试功能

未来可以进一步优化的方向包括：增强权限审计功能、提供更丰富的权限模板、支持动态权限调整等。